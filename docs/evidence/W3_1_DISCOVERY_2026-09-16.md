# CORE W3.1 — Production DDL Discovery

Date: 2026-09-16
Branch: `feat/core-w3-1-ddl-authority`

## Finding

The repository has a PostgreSQL-backed W3 schema authority, but no established production migration executor was identified in the inspected code/search surface.

Observed schema initialization paths include SQLAlchemy metadata `create_all` and a separate SQLite-oriented direct-DDL path. These paths are not yet proven to be delegated through the canonical W3 schema authority.

## Assurance consequence

`GC-SCH-003` (canonical schema/version authority) remains bounded GREEN from W3 evidence.

`GC-SCH-004` (production DDL migration execution authority) remains OPEN pending architecture ownership, implementation, adversarial PostgreSQL validation, independent re-performance, and actual-schema reconciliation.

This discovery is not a CORE failure by itself. It is an explicitly bounded architectural gap.

## Required next evidence

1. complete production DDL path inventory;
2. declare which path is production-authoritative;
3. implement or integrate the migration executor without creating duplicate schema authority;
4. verify actual PostgreSQL schema against canonical schema state;
5. execute W3.1 falsification suite;
6. reproduce independently;
7. reconcile evidence to the exact execution commit.

## Scope

SHUUD is excluded from this workstream and must not be used to close W3.1 or any CORE gate.
